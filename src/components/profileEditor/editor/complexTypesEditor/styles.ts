import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  attributeElement: {
    marginBottom: 16
  },
  accordionMultiple: {
    marginBottom: 12
  },
  multipleComplexType: {
    marginBottom: 4
  }
}));
